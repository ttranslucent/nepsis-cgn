# Session Ledger - 2026-07-17 (Session 52)

## What We Worked On

- Closed the CGN side of the local NepsisMC/NepsisCGN operator-stack
  integration by hardening private-runtime initialization and connecting MC
  only to the governance-profile service.
- Preserved MC as the canonical writer and kept the private runtime separate
  from the deterministic, model-free public `/mvp` path.

## What We Got Done

- Added `nepsiscgn-init-private-runs` through
  `src/nepsis_cgn/canonical_runs/private_runtime_init.py`. It creates a new
  mode-`0700` durable root, mode-`0600` stores, key, and environment files, an
  Ed25519 signing key, an active append-only trust-anchor ledger, and four
  distinct capability tokens. It refuses relative, temporary, existing, or
  partial targets and does not print tokens.
- Updated `src/nepsis_cgn/canonical_runs/private_runtime.py` to require a
  distinct profile-only credential with only `revise_operator_profile`. Tests
  and raw capability checks proved that it receives `403` for canonical-run
  creation, snapshot reads, export, STILL release, and decision-commit
  requests.
- Initialized the local runtime and created and activated `profile-local`
  revision 1. MC receives only `mc-profile.env`, with
  `NEPSISMC_RUNTIME_MODE=legacy`; it receives no operator, model, or validator
  credential.
- Ran the separate synthetic, non-authoritative CGN evaluation: one STILL
  release and one validator commit were observed, CGN and independent MC export
  verification passed, and a stale head was refused. The evaluation kept
  `adoption_eligible: false`, left `mc_writer` unchanged, and created or
  activated no cutover marker.
- Updated `docs/canonical-operator-run-contract.md` and added initializer and
  profile-boundary regression coverage in `tests/test_private_runtime_init.py`
  and `tests/test_private_runtime.py`.

## What We Need To Do Next

- No implementation change remains for this local integration closeout.
- Do not activate `cgn_writer` from this evidence alone. Any authority cutover
  still requires a separate adoption decision, signed cutover evidence,
  durable backup/recovery review, and rollback review.
- Treat hosted or multi-user private writing as a separate identity, tenancy,
  storage, and threat-model contract.

## Overall Progress

- The private CGN runtime and MC profile connection are working locally, but
  CGN canonical-run authority remains inactive and MC remains the writer.
- Full Python suite: `559 passed`; focused public-boundary suite: `82 passed`;
  Ruff, Black, web lint, and the Next.js production build passed.
- Push-triggered GitHub CI passed for implementation commit `1829251`; before
  this ledger-only follow-up, local `main` exactly matched `origin/main`.
- The private runtime continues to return `404` for `/v1/mvp` and
  `/openapi.json`; no public `/mvp` behavior or model-free boundary changed.

# Session 48 - 2026-03-17

## Scope
- Finish the remaining Vercel-facing deployment hardening items for `nepsis-web`.
- Make production engine connectivity fail clearly when `NEPSIS_API_BASE_URL` is missing.
- Make passwordless login explain missing email delivery configuration instead of implying mail was sent.

## Changes
- Hardened `nepsis-web/src/lib/engineApi.ts` so production requires `NEPSIS_API_BASE_URL`; development still falls back to `http://127.0.0.1:8787`.
- Added shared engine proxy error handling and updated `/api/engine/*` routes to return a structured `503` when engine base URL is missing.
- Updated `nepsis-web/src/lib/useEngineSession.ts` and `nepsis-web/src/app/engine/page.tsx` so `/engine` shows `not configured` instead of generic unreachable status when deployment env is incomplete.
- Updated `nepsis-web/src/lib/nepsisAuth.ts` and `nepsis-web/src/app/login/page.tsx` so login messaging explicitly calls out missing `RESEND_API_KEY` and `NEPSIS_AUTH_FROM_EMAIL`, while keeping preview-code mode accurate for non-production/testing.
- Replaced `nepsis-web/.env.example` with a deployment-oriented example covering engine, auth, OpenAI, and proto-puzzle env vars.
- Rewrote `nepsis-web/README.md` with project-specific local-dev and Vercel deployment instructions.

## Validation Plan
- Run `npm run lint` in `nepsis-web`.
- Run `npm run build` in `nepsis-web`.
- Confirm `/api/engine/health` returns a clear config error when `NEPSIS_API_BASE_URL` is absent in production mode.

# Session Ledger - 2026-02-28 (Session 16)

## What We Worked On

- Closed three backend/API production gaps:
  - Durable session persistence
  - Explicit `p_bad` calibration wiring
  - Auth/permission controls for engine routes

## What We Got Done

- Added disk-backed session persistence + replay in `src/nepsis_cgn/api/service.py`:
  - `EngineApiService(store_path=...)` with JSON store load/save
  - Action-log replay (`step` + `reframe`) to rebuild navigation state and packets
  - Persist on create/step/reframe/delete
  - Session summary now exposes storage mode + governance/calibration config
- Added explicit calibration object in `src/nepsis_cgn/core/governance.py`:
  - `GovernanceCalibration` with prior/logit + feature weights
  - `probability(...)` built on `posterior_from_score(...)`
- Wired calibration through runtime/navigation:
  - `src/nepsis_cgn/core/runtime.py` accepts `governance_calibration`
  - `src/nepsis_cgn/core/navigation.py` uses calibration for `p_bad`
- Added backend route protection in `src/nepsis_cgn/api/server.py`:
  - Token checks on control routes via `Authorization: Bearer` or `X-API-Key`
  - `/v1/health` + `/v1/routes` remain public
  - CORS allows auth headers
- Fixed port suggestion UX:
  - `_bind_error_message` now suggests an actually available nearby port when possible
- Added Next.js control-route auth in `nepsis-web/src/lib/engineApi.ts` + engine route handlers:
  - Requires `nepsis_user` cookie on `/api/engine/sessions*`
  - Forwards backend token automatically when `NEPSIS_API_TOKEN` is set
- Adjusted login cookie behavior for local dev in `nepsis-web/src/app/api/auth/verify-code/route.ts`:
  - `secure` now depends on `NODE_ENV === "production"`

## What We Need To Do Next

- Add TTL-based session cleanup policy on the persisted session store.
- Add stronger auth model (RBAC/role claims) beyond presence of `nepsis_user` cookie.
- Replace hand-tuned calibration coefficients with dataset-driven fit + calibration report tooling.
- Add OpenAPI/route docs from the running server manifest for API discoverability.

## Overall Progress

- The engine API is no longer memory-only in normal server operation.
- `p_bad` is now governed by explicit, inspectable calibration parameters.
- Engine control routes now have both frontend cookie gating and backend token enforcement (when token configured).

Validation:

- `PYTHONPATH=src python3 -m pytest -q tests/test_engine_api_service.py tests/test_engine_api_server.py tests/test_governance.py tests/test_navigation_packet.py` -> `34 passed`

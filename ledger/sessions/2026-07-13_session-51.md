# Session Ledger - 2026-07-13 (Session 51)

## What We Worked On

- Built the inactive private canonical-run authority needed for the reviewed
  NepsisMC/NepsisCGN split.
- Preserved Codex App Server as conversation/history, NepsisMC as the
  trust-bearing cockpit/client, and public `/mvp` as a separate deterministic,
  model-free path.

## What We Got Done

- Added the durable append-only run ledger, immutable artifacts, atomic
  projections/outcomes, challenged snapshots, Ed25519 receipts, trust-anchor
  lifecycle, and detached protected-export verifier.
- Added immutable versioned governance profiles and per-run policy snapshots.
- Added exact proposal disposition without application, explicit STILL release,
  validator-owned exact proposal application, and ZeroBack protected-root
  preservation.
- Added atomic predecessor freeze plus lineage-bound successor creation for
  irrecoverable conversation recovery, with independent parent/child pair
  verification.
- Added authenticated real-HTTP tests covering candidate -> accept -> release ->
  commit, validator/operator identity binding, refusals, stale heads, export,
  and a separate ZeroBack recovery flow.
- Kept the private runtime isolated from public `/mvp`, docs discovery, and
  OpenAPI discovery.

## What We Need To Do Next

- Do not activate `cgn_writer` from this implementation alone.
- Before any cutover decision, manually review the signed capability-policy,
  adoption evidence, durable-path operations, backup/recovery posture, and
  rollback marker.
- Treat hosted or multi-user private writing as a separate threat, identity,
  tenancy, and storage contract.

## Overall Progress

- Private canonical authority is implemented and adversarially verified as an
  inactive candidate; it is not live authority.
- Full Python suite: `540 passed`.
- Focused public `/mvp` boundary: `124 passed`.
- Next.js public/operator UI lint and production build passed.
- No public `/mvp` behavior, fallback packet, or model-free boundary changed.

# Session Ledger - 2026-03-01 (Session 23)

## What We Worked On

- Simplified `/engine` UX for first-time users while preserving full operator controls.

## What We Got Done

- Added dual-mode UI:
  - **User Mode**: guided stage-driven flow with progressive revelation.
  - **Operator Mode**: full control room with session controls and diagnostics.
- Implemented stage-driven progressive layout:
  - Stage 1 shows frame setup.
  - Stage 2 revealed after frame lock.
  - Stage 3 revealed after report lock.
  - Prior stages collapse to compact summaries in User Mode.
- Moved detached chat into a **slide-out Model Sandbox drawer**:
  - model selector,
  - multi-model compare toggle,
  - optional input source.
- Added posterior hierarchy in Stage 3:
  - Decision,
  - Ruin status,
  - Gate result (`p_bad` vs `theta`),
  - posterior chart,
  - carry-forward editing.
- Reworked timeline to a compact horizontal strip:
  - mixed frame + packet events (`RESET/CALL/REPORT/EVALUATE/COMMIT`),
  - click-to-inspect detail panel.
- Hid debug/session artifacts behind a **System Status** toggle.

## What We Need To Do Next

- Connect sandbox drawer to real provider adapters (currently local detached transcript behavior).
- Add responsive/mobile stepper polish for User Mode.
- Add role-based defaults so non-operators always start in User Mode.

## Overall Progress

- `/engine` has shifted from “control room only” to a product-ready guided surface with optional advanced mode.
- Build status:
  - `npm run build` in `nepsis-web` passed.

# Session Ledger - 2026-03-01 (Session 22)

## What We Worked On

- Refined `/engine` UI/UX to match sketch and concrete workflow guidance.

## What We Got Done

- Added a **detached Chat UX rail** (left side) with:
  - model selector,
  - multi-model comparison toggle,
  - optional input source field,
  - separate transcript that does not alter Nepsis packet state.
- Made **Priors / Frame collapsible**:
  - explicit collapse/expand control,
  - auto-collapse on frame lock,
  - quick frame summary when collapsed.
- Updated **Posterior panel trust indicators** to be explicit and readable:
  - most likely interpretation + margin,
  - ruin flags + ruin mass,
  - threshold gate math (`p_bad` vs `theta` and crossed/not crossed).
- Added **bottom packet event timeline strip**:
  - visual sequence of packet events (`RESET/CALL/REPORT/EVALUATE/COMMIT`),
  - clickable event chips,
  - event inspection details (stage, iteration, decision, cause, carry-forward policy).
- Kept existing backend wiring and stage gating intact.

## What We Need To Do Next

- Hook detached chat rail to real provider adapters (currently local sidecar behavior only).
- Add compact/mobile tab behavior for the new side rail + three panels.
- Add timeline filters (by iteration, severity, trigger code).

## Overall Progress

- Frontend now better reflects Nepsis interaction model:
  - conversational and human-readable,
  - auditable through explicit event timelines,
  - reduced JSON-heavy cognitive load.
- Build status:
  - `npm run build` in `nepsis-web` passed.

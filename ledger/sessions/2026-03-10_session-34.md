# Session Ledger - 2026-03-10 (Session 34)

## What We Worked On

- Moved lineage metadata from frontend-local timeline state into backend-native session state.
- Wired Engine UI to consume backend lineage fields as source-of-truth.

## What We Got Done

- Backend lineage persistence (`src/nepsis_cgn/api/service.py`):
  - Added session-level lineage fields:
    - `branch_id`
    - `lineage_version`
    - `parent_frame_id`
    - `frame_ref` (response helper)
  - Extended `reframe_session(...)` to accept optional:
    - `branch_id`
    - `parent_frame_id`
  - Reframe now updates and returns lineage metadata with frame/stage.
  - Session summaries (create/list/get/step session payload) now include lineage metadata.
  - Added SQLite schema migration support for new columns:
    - `branch_id`
    - `lineage_version`
    - `parent_frame_id`
  - Bumped store schema versions:
    - JSON schema `1.1.0`
    - SQLite schema `2`
- API adapters:
  - Added request validation and passthrough for `branch_id` and `parent_frame_id` in:
    - `src/nepsis_cgn/api/server.py`
    - `src/nepsis_cgn/api/asgi.py`
- Frontend typed client/hook updates:
  - Extended `EngineSessionSummary` and reframe response typing with lineage fields in:
    - `nepsis-web/src/lib/engineClient.ts`
  - Updated `useEngineSession().reframe(...)` to return full reframe response and merge lineage fields into active session in:
    - `nepsis-web/src/lib/useEngineSession.ts`
- Engine page integration (`nepsis-web/src/app/engine/page.tsx`):
  - Timeline metadata model now supports backend `lineageVersion` and `parentFrameId`.
  - Open/create/reframe/commit flows now ingest and propagate backend lineage fields.
  - Reframe calls now send `branch_id` and `parent_frame_id`.
  - Branch UI state syncs from active session metadata.

## Validation

- Backend tests:
  - `.venv/bin/pytest tests/test_engine_api_service.py tests/test_engine_api_server.py -q`
  - Result: `35 passed`
- Frontend build:
  - `npm run build` (in `nepsis-web`)
  - Result: passed

## What We Need To Do Next

- Persist full frame lineage graph (not just current-session lineage pointers) so branch browsing survives reload across all historical versions.
- Add explicit backend endpoint for lineage history retrieval (frame DAG/timeline) instead of relying on UI-assembled timeline entries.
- Extend multi-model comparison to persisted, provider-backed run artifacts with structured divergence fields.

## Overall Progress

- Lineage is no longer frontend-only metadata; it is now part of canonical backend session state and survives persistence/reload.
- Engine UI now consumes canonical lineage fields and sends explicit lineage intent during reframe transitions.
